avidia_banking_governance:
  target: prod
  outputs:
    prod:
      type: snowflake
      database: ANALYTICS
      schema: MARTS
      warehouse: WH_GOVERNANCE_XS
      role: SVC_PIPELINE_ROLE
      threads: 4
